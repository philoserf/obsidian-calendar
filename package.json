{
  "name": "calendar",
  "version": "3.0.0",
  "main": "main.js",
  "description": "Calendar view of your daily, weekly, and monthly notes with metadata tracking for tasks, tags, word count, and streaks",
  "author": "Mark Ayers (originally by Liam Cain)",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "bun run check && vite build",
    "check": "bun run typecheck && biome check . && svelte-check",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "validate": "bun run scripts/validate-plugin.ts",
    "test": "bun test",
    "version": "bun run version-bump.ts",
    "deploy": "cp main.js manifest.json styles.css ~/source/philoserf/notes/.obsidian/plugins/calendar/"
  },
  "dependencies": {
    "svelte": "^5.53.7"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "@sveltejs/vite-plugin-svelte": "^6.2.4",
    "@tsconfig/svelte": "^5.0.8",
    "@types/bun": "^1.3.10",
    "@types/node": "^25.3.5",
    "moment": "^2.30.1",
    "obsidian": "^1.12.3",
    "svelte-check": "^4.4.5",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
